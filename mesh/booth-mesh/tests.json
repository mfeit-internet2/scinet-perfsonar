{
    "booth-reachability": {
        "type": "netreach",
	"spec": {
	    "host": "{% jq .addresses[0]._meta.dnochost %}",
	    "network": "{% jq .addresses[1]._meta.network %}",
	    "scan": "edges",
	    "schema": 1,
	    "timeout": "PT10S"
        }
    }
}
